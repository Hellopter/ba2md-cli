# 3.x.2.1 Overall Solution Design · Required

Read this constraint for every selected feature.

## Writing Requirements

- Describe the current main path and target main path separately.
- Cover caller, authorization, validation, orchestration, persistence, integration, and response.
- Explain state invariants, transaction and consistency boundaries, concurrency, failures, and compensation.
- Place precise evidence/claim IDs next to load-bearing steps.
- Use Mermaid when sequence, state, or boundaries are complex.

## Output Format

```markdown
#### 3.x.2.1 Overall Solution Design

- Current main path (FACT):
- Target main path (PROPOSAL):
- Design decisions and alternatives (DECISION):
- Key states and invariants:
- Transaction / consistency boundary:
- Failure and compensation path:
- Evidence / Claim IDs:
```

## Quality Gate

- The solution is directly implementable through existing seams.
- Success and failure paths are complete.
- Existing identifiers are VERIFIED; new names are explicitly marked PROPOSAL.
