# 3.x.2.3 Internal Interface Design · Conditional

Resolve this section in the Template Coverage Plan before deep research. Read this constraint when module/service commands, internal APIs, domain events, or callable symbols may change. When `NOT_APPLICABLE`, omit the body or write N/A with a reason as the active template permits.

## Research and Inventory

- Search all relevant module, command, service, event, and callable-symbol locations; do not stop at the first match.
- Record every discovered interface in the Implementation Inventory as in scope or excluded with a reason.
- State caller-to-provider direction and synchronous/asynchronous semantics.
- Compare each current symbol or contract with the target change.
- Cover changed fields, load-bearing fields, result, errors, timeout, idempotency, and authorization.
- State compatibility strategy and change type.

## Output Format

```markdown
#### 3.x.2.3 Internal Interface Design

| Interface / symbol | Call direction | Current contract | Target change | Error / timeout / idempotency | Authorization | Change type | Evidence / Claim ID |
|--------------------|----------------|------------------|---------------|-------------------------------|---------------|-------------|---------------------|
| | | | | | | | |
```

## Quality Gate

- Every in-scope internal interface from the inventory appears in the table; the section is not a representative sample.
- Symbols and call direction match production source.
- Fields and errors are not inferred from Java test sources.
- Every omitted discovered interface has an explicit exclusion reason.
- Every new interface includes evidence that existing seams are insufficient.
