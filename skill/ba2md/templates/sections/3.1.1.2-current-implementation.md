# 3.x.1.2 Current Implementation (FACT) · Required

Read this constraint for every selected feature. Describe only current production implementation supported by VERIFIED FACTs.

## Source Exclusions

Completely ignore Java test sources: exact filename `test.java`, `**/src/test/**`, `**/*Test.java`, `**/*Tests.java`, and `**/*IT.java`. Do not inspect, cite, count, summarize, or infer production behavior from them.

## Research Questions

- What are the real entry points and call chain?
- Which contracts, fields, symbols, schemas, permissions, limits, alerts, and configuration currently exist?
- Which existing seam can support this requirement?
- Which relevant material remains missing after a bounded search?

## Output Format

```markdown
#### 3.x.1.2 Current Implementation (FACT)

| Concern | Current implementation | Requirement gap | Evidence ID |
|---------|------------------------|-----------------|-------------|
| | | | |
```

## Quality Gate

- Every precise current identifier resolves to VERIFIED raw evidence.
- Wiki SUMMARY does not prove implementation details.
- No Java test source appears in the evidence set.
- Missing facts are GAPs, never guesses.
