interface:
  display_name: "ba2md"
  short_description: "Generate source-grounded detailed design documents"
  default_prompt: "Use $ba2md to turn Markdown requirements into a source-grounded SDD. Select plausible wiki/source project pairs, verify raw implementation evidence, write a transparent draft with GAPs instead of guesses, review it with the user, apply feedback, and finalize only after explicit confirmation."
